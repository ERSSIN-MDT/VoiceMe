{
  "name": "voiceme",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": { "dev": "node --env-file-if-exists=.env.local server.js", "dev:vercel": "vercel dev", "deploy": "vercel --prod" },
  "engines": { "node": ">=20" }
}
