async function request(url, options={}){const response=await fetch(url,{...options,headers:{'content-type':'application/json',...(options.headers||{})}});const data=await response.json().catch(()=>({}));if(!response.ok)throw new Error(data.error||'The service is unavailable.');return data}
export const api={room:(action,body)=>request('/api/room',{method:'POST',body:JSON.stringify({action,...body})}),send:body=>request('/api/signal',{method:'POST',body:JSON.stringify(body)}),poll:({room,id})=>request(`/api/signal?room=${encodeURIComponent(room)}&id=${encodeURIComponent(id)}`),ice:()=>request('/api/ice')};
